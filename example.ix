set square [ shift | mul . . ]

# these are equivalent

# prefix notation (better for files)
puts (square 5)

# postfix notation (better for the shell)
square 5 | puts .

set foo '{abcdef {sdflkj} {}}
set rightBrace '{\}}
set leftBrace '{\{}
set backslash '{\\}
